//
//  officastUITests.swift
//  officastUITests
//
//  The real end-to-end flows live in the feature-specific files
//  (Onboarding/Dashboard/Calendar/Report/Settings/NotificationCheckIn), all
//  built on `UITestCase`. This file intentionally holds no tests; the Xcode
//  template's `testExample` / `testLaunchPerformance` were removed because they
//  launch without the hermetic `UITEST` flag (live network, non-deterministic).
//
